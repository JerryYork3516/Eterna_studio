"""Schema definitions package.

Currently hosts the Digital Resident (DR) v0.2 policy-layer schema. These are
PURE declarative configuration models — they describe a resident's policy
surface and carry no runtime, scheduling, or execution behavior.
"""
